//Modal con alerta para registrarse, en vista login.
document.getElementById("modalRegistrate").addEventListener("click", function(event) {
    event.preventDefault(); // Evita la redirección predeterminada del enlace

    // Muestra una alerta
    alert("¡Contacta al administrador de prevención para solicitar la creación de una nueva cuenta de usuario!");
});
